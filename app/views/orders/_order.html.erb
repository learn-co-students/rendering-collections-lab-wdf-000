<% order.products.each do |product| %>
  <tr>
    <td><%= order.id %></td>
    <td><%= product.name %></td>
    <td><%= product.price %></td>
    <td><%= order.customer.name %></td>
  </tr>
<% end %>

<% if @orders[0] %>
  
<% else %>
  <h3>No Orders to show</h3>
<% end %>